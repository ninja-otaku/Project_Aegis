fastapi>=0.115
uvicorn[standard]>=0.30
opencv-python>=4.9
numpy>=1.26
pydantic-settings>=2.3

# AI providers — install only what you need
anthropic>=0.40              # AI_PROVIDER=claude
google-generativeai>=0.8     # AI_PROVIDER=gemini
openai>=1.0.0                # AI_PROVIDER=openai
ollama>=0.2.0                # AI_PROVIDER=ollama  (also requires Ollama running locally)
mistralai>=1.0.0             # AI_PROVIDER=mistral
groq>=0.9.0                  # AI_PROVIDER=groq

# Optional features
pyttsx3>=2.90                # TTS_ENABLED=true
trustme>=0.9                 # for scripts/generate_cert.py (TLS setup)

# Tests
pytest>=8.0                  # run: pytest tests/
